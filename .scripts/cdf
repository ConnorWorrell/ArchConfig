directories="$(ls -l | grep -i "^d.*$1" | cut -d\  -f19- )"
echo "$(echo "$directories" | wc -l)"
[ "$(echo "$directories" | wc -l)" -gt "1" ] && directories="$(echo "$directories" | dmenu -i)"
cd "$directories"
